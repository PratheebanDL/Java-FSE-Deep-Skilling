<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Enables scanning of com.library and its sub-packages for
         @Component, @Service, @Repository, and @Controller annotated classes.
         Spring will auto-register beans for BookService and BookRepository,
         and auto-wire dependencies marked with @Autowired.
         No manual <bean> declarations are needed anymore. -->
    <context:component-scan base-package="com.library" />

</beans>
