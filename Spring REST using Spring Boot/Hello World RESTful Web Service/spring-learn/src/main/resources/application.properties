# Embedded Tomcat server port.
# The Hello World REST service must be reachable at http://localhost:8083/hello
server.port=8083

# Application name (shown in logs)
spring.application.name=spring-learn
